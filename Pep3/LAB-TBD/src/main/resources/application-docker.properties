spring.application.name = LAB-TBD

server.port = 8097

# Configuración de base de datos para Docker
spring.datasource.url = ${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres-db:5432/LabTBD-2-2024}
spring.datasource.username = ${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password = ${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driverClassName = org.postgresql.Driver
spring.jpa.hibernate.ddl-auto = create-drop

jwt.secret = ${JWT_SECRET:OvmAbqM826GczKY1yF2MyYDjnAVcwKMPnL+OcobQ7bWDwf0IGy1YySsRTepJOI8j}

# Spring Boot Actuator Configuration
management.endpoints.web.exposure.include = health,info,prometheus,metrics
management.endpoint.health.show-details = always
management.endpoint.prometheus.enabled = true
management.metrics.export.prometheus.enabled = true
management.endpoints.web.base-path = /actuator

# Micrometer Configuration
management.metrics.tags.application = ${spring.application.name}

# Logging configuration para Docker
logging.level.org.springframework.web = INFO
logging.level.G1TBD.LABTBD = INFO
logging.level.org.springframework.boot.autoconfigure = WARN 